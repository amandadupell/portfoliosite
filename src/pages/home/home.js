import React from 'react';
import { Container } from './constants';

class Home extends React.Component {
    render() {
        return (
            <Container></Container>
        );
    }
};

export default Home;
